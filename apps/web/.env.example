# Absolute API origin for the typed client. Leave empty in dev so the browser
# uses the same origin and Vite proxies /v1, /health and /metrics to the API
# (uvicorn on :8000 via `make run`). Set to e.g. https://api.example.com in prod.
VITE_API_BASE=
