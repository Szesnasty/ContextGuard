{
  "$defs": {
    "UserContext": {
      "additionalProperties": false,
      "description": "Who is asking, and under what authority \u2014 the input to policy.",
      "properties": {
        "labels": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Labels",
          "type": "array"
        },
        "purpose": {
          "title": "Purpose",
          "type": "string"
        },
        "role": {
          "title": "Role",
          "type": "string"
        },
        "sub": {
          "title": "Sub",
          "type": "string"
        },
        "tenant": {
          "title": "Tenant",
          "type": "string"
        }
      },
      "required": [
        "sub",
        "tenant",
        "role",
        "purpose"
      ],
      "title": "UserContext",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "A RAG query: who is asking, what they ask, how many chunks to retrieve.",
  "properties": {
    "k": {
      "default": 5,
      "maximum": 50,
      "minimum": 1,
      "title": "K",
      "type": "integer"
    },
    "query": {
      "minLength": 1,
      "title": "Query",
      "type": "string"
    },
    "user": {
      "$ref": "#/$defs/UserContext"
    }
  },
  "required": [
    "query",
    "user"
  ],
  "title": "QueryRequest",
  "type": "object"
}
